# Core
torch>=2.1
torchvision>=0.16
numpy>=1.24
pillow>=9.0
wandb>=0.16

# Optional — real-environment evaluation (dynamics/evaluate_env.py):
#   pip install "dm_control>=1.0.18" "mujoco>=3.1"
# Optional — LPIPS perceptual loss for tokenizer training (auto-disabled if absent):
#   pip install lpips
# Optional — extra image I/O utilities:
#   pip install "opencv-python>=4.8"
